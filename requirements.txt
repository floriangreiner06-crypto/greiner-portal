# ============================================================================
# Bankenspiegel 3.0 - Python Dependencies
# ============================================================================
# Installation: pip install -r requirements.txt
# ============================================================================

# PDF: Parsing + Generierung (Reporting, Testanleitungen, Provision)
pdfplumber==0.11.0
reportlab>=4.0.0
# Fahrzeuganlage: PDF-Upload (erste Seite → Bild für OCR)
pymupdf>=1.24.0

# Flask (fÃ¼r Web-Interface)
Flask==3.0.0

# Datenbank
# sqlite3 ist in Python Standard Library enthalten

# Optional aber empfohlen:
python-dateutil==2.8.2

# .env laden (z. B. für TEK-Script, DB_Credentials)
python-dotenv>=1.0.0

# FÃ¼r Produktiv-Deployment (optional)
gunicorn==21.2.0
beautifulsoup4==4.12.2

# HTTP Requests (für WhatsApp API, etc.)
requests>=2.31.0

# Twilio WhatsApp API (TAG 211)
twilio>=8.0.0

# Redis für Caching (TAG 213)
redis>=5.0.0

# Excel-Export (z. B. VFW Lohnsteuerliste)
openpyxl>=3.1.0

# LDAP/AD (Sync ldap_employee_mapping, Auth)
ldap3>=2.9.1
